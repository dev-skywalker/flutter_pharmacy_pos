import 'package:flutter/material.dart';

class UnitReportPage extends StatelessWidget {
  const UnitReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Unit Report"),
      ),
    );
  }
}
