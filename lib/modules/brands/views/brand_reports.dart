import 'package:flutter/material.dart';

class BrandReportPage extends StatelessWidget {
  const BrandReportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Brand Report"),
      ),
    );
  }
}
